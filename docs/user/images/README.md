# Image placeholders

This directory holds screenshots and diagrams referenced from `README.md`, `README.ja.md`, and `docs/user/`.

Expected files:

- `overview.png` — wide hero shot showing an editor with a CustomViewer preview beside it.
- `quickstart.png` — the result of the 30-second quick-start using the `handbook.md` sample.
- `choose-renderer.png` — the "Choose Renderer Preview" picker with multiple entries.
- `restricted-mode.png` — VS Code Restricted Mode banner with CustomViewer behavior.

Add the image files here with the names above. Markdown links already reference them, so the docs render correctly as soon as the files exist.
